# https://gitlab.com/bitcoinunlimited/buildAndTestVMs
image: andrewstone/nexaadk:latest

# These "stages" are actually separately initiated VMs, so better used for different configurations, etc
stages:
  - mirroring
  - docs
  - lint
  - test
  - coverage
  - assemble
  - build # This job should be last before deploy because it runs for one hour. This job can be decoupled (see taskTree).
  - deploy_internally
  - deploy_beta

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: always
    - if: $CI_COMMIT_REF_PROTECTED == "true"
      when: always

# rules snippet template to use if you want that a task is skipped
# on the scheduled pipeline used for mirroring to github.com
# NB even if you include this snippet in a specif task, local (to the
# task) subsequent rules definition will overwrite the snippet.
# e.g. task like build or testflight need ad hoc modification to
# their rules section rather than including the following snippet
# in the task definition
.skip-on-schedule: &skip-on-schedule
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule" && $MIRROR == "True"'
      when: never
    - when: on_success

.skip-on-schedule-fragment: &skip-on-schedule-fragment
    if: '$CI_PIPELINE_SOURCE == "schedule" && $MIRROR == "True"'
    when: never

before_script:
  - GIT_COMMIT_HASH=$(git rev-parse --short HEAD)
  - export GIT_COMMIT_HASH
  - 'echo "Current Git commit hash: $GIT_COMMIT_HASH"'
  - ./gradlew generateVersionFile

variables:
  VERSION_CODE_RAW: ${CI_PIPELINE_IID}+${VERSION_BUMPER}
  VERSION_CODE_RENDER: $(($VERSION_CODE_RAW))

lintVitalRelease:
  <<: *skip-on-schedule
  stage: lint
  script:
    - ./gradlew :src:lintVitalRelease

lint:
  <<: *skip-on-schedule
  stage: lint
  script:
    - ./gradlew lint -x :src:lintDebug # Skips android linter with 356 errors, 500 warnings.

defaultGradle:
  <<: *skip-on-schedule
  stage: lint
  script:
    - ./gradlew --info

appJar:
  <<: *skip-on-schedule
  stage: assemble
  script:
    - ./gradlew appJar
  artifacts:
    paths:
      - src/build/libs/wpw.jar

build:
  stage: build
  tags:
    - m1-baremetal
  rules: # this job is very slow (90 min ++). Therefore we restrict this job to run only on MR approval and always on the main branch or triggered manually.
    # Never run on scheduled pipeline used to mirror to github
    # - *skip-on-schedule-fragment
    # Run the job on the main branch
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: on_success # Will only run if all previous jobs in the pipeline complete successfully.
    # Run the job if the merge request is approved
    - if: '$CI_MERGE_REQUEST_APPROVED == "true"'
      when: on_success # Will only run if all previous jobs in the pipeline complete successfully.
    # Allow manual execution for merge request pipelines
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    # Allow manual execution as a fallback if the above rules are not met
    - when: manual
  variables:
    ANDROID_HOME: "/opt/homebrew/share/android-commandlinetools"
  script:
    # Exclude the task :src:testDebugUnitTest and :src:testReleaseUnitTest because (I think) they do not support multiplatform projects
    - ./gradlew build -x :src:testDebugUnitTest -x :src:testReleaseUnitTest
  artifacts:
    when: always
    reports:
      junit:
        - src/build/test-results/jvmTest/TEST-*.xml
        - src/build/test-results/iosSimulatorArm64Test/TEST-*.xml
        - src/build/test-results/iosArm64Test/TEST-*.xml
    paths:
      - src/build/reports/tests/testDebugUnitTest/index.html
      - src/build/reports/tests/testDebugUnitTest/**/**.html
      - src/build/reports/tests/iosSimulatorArm64Test/index.html
      - src/build/reports/tests/iosSimulatorArm64Test/**/**.html
      - src/build/reports/lint-results-debug.html
      - src/build/reports/tests/allTests/index.html
      - build/reports/problems/problems-report.html

compileIos:
  <<: *skip-on-schedule
  stage: assemble
  tags:
    - m1-baremetal
  script:
    - ./gradlew :src:compileKotlinIosArm64
    - ./gradlew :src:compileKotlinIosSimulatorArm64
    - ./gradlew :src:iosArm64MetadataElements
    - ./gradlew :src:iosSimulatorArm64MetadataElements

jvmJar:
  <<: *skip-on-schedule
  stage: assemble
  tags:
    - m1-baremetal
  script:
    - ./gradlew :src:jvmJar

iosArm64Test:
  <<: *skip-on-schedule
  stage: test
  tags:
    - m1-baremetal
  resource_group: iphone_mac_emulators
  script:
    - ./gradlew :src:cleanIosSimulatorArm64Test :src:iosSimulatorArm64Test
  artifacts:
    when: always
    paths:
      - src/build/reports/tests/iosSimulatorArm64Test/index.html
      - src/build/reports/tests/iosSimulatorArm64Test/**/**.html

iosEmulatorUiTest:
  <<: *skip-on-schedule
  stage: test
  resource_group: iphone_mac_emulator
  tags:
    - m1-baremetal
  script:
    - cd iosApp
    - gem install bundler
    - gem install fastlane
    - bundle install
    - bundle update
    - bundle exec fastlane ios test_emulator
  artifacts:
    when: always
    reports:
      junit: iosApp/fastlane/report.xml
    paths:
      - iosApp/fastlane/report.xml
      - iosApp/fastlane/test_output/report.html
      - iosApp/fastlane/test_output/report.junit

iosDeviceUiTest:
  <<: *skip-on-schedule
  stage: test
  resource_group: iphone_mac_emulator
  tags:
    - m1-baremetal
  script:
    - cd iosApp
    - gem install xcpretty
    - ./rundevicetests.sh $DEVICE_UDID
  artifacts:
    when: always
    # The .xcresult bundle can contain console logs, screenshots and absolute
    # runner paths, so restrict downloads to developer-role users and up.
    access: developer
    reports:
      junit: iosApp/iphone-ui-test-report.xml
    paths:
      - iosApp/build/Test.xcresult
    expire_in: 1 week

jvmTest-ubuntu-docker:
  <<: *skip-on-schedule
  stage: test
  tags:
    - berlin
  script:
    - ./gradlew :src:cleanJvmTest :src:jvmTest
  artifacts:
    reports:
      junit: src/build/test-results/jvmTest/TEST-*.xml
    when: always
    paths:
      - build/reports/problems/problems-report.html

jvmTestCoverageXml:
  <<: *skip-on-schedule
  stage: test
  script:
    - ./gradlew :src:koverXmlReportJvm # koverXmlReportJvm runs jvmTest as a child job
  artifacts:
    reports:
      coverage_report:
        coverage_format: jacoco
        path: src/build/reports/kover/reportJvm.xml
      junit: src/build/test-results/jvmTest/TEST-*.xml
      # cobertura: src/build/reports/kover/reportJvm.xml
      # Maybe this? https://docs.gitlab.com/ee/ci/testing/test_coverage_visualization/jacoco.html
    paths:
      - src/build/reports/kover/reportJvm.xml
      - src/build/reports/tests/jvmTest/**/**

jvmTestCoverageLog: #
  <<: *skip-on-schedule
  stage: test
  script:
    - ./gradlew :src:koverLogJvm # koverLogJvm runs jvmTest as a child job
  coverage: '/application line coverage: (\d+\.\d+)%/' # Regex to extract coverage percentage
  artifacts:
    reports:
      junit: src/build/test-results/jvmTest/TEST-*.xml
    paths:
      - build/reports/problems/problems-report.html
      - src/build/reports/tests/jvmTest/**/**

# Runs Compose UI tests for Android on a Pixel 5 emulator. LeakCanary heap analysis is OFF here
# (LEAK_DETECTION unset) so the run stays fast. Use androidPixel5TestLeakDetection for leak checks.
androidPixel5Test:
  <<: *skip-on-schedule
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule" && $MIRROR == "True"'
      when: never
      # run on main branch
    - if: $CI_COMMIT_REF_PROTECTED == "true"
      when: never
  stage: test
  tags:
    - m1-baremetal # default docker image is not working
  resource_group: pixel5_mac_emulators
  script:
    - ./gradlew pixel5Check --full-stacktrace
  artifacts:
    reports:
      junit: src/build/outputs/androidTest-results/managedDevice/debug/pixel5/TEST-*.xml
    when: always
    paths:
      - src/build/reports/androidTests/managedDevice/debug/pixel5/index.html
      - src/build/reports/androidTests/managedDevice/debug/pixel5/**/**
      - src/build/outputs/androidTest-results/managedDevice/debug/pixel5/**/**
      - src/build/intermediates/managed_device_android_test_additional_output/pixel5/debug/**/**

# Same Pixel 5 Compose UI tests, but with LEAK_DETECTION=true so LeakCanary runs a heap analysis
# after each passing test and fails on any retained instance. Expect ~5-10s/test slower than
# androidPixel5Test. Kept as a separate, clearly named job so the slowdown is never a surprise.
androidPixel5TestLeakDetection:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule" && $MIRROR == "True"'
      when: always
      # run on main branch
    - if: $CI_COMMIT_REF_PROTECTED == "true"
      when: always
  stage: test
  tags:
    - m1-baremetal # default docker image is not working
  resource_group: pixel5_mac_emulators
  variables:
    LEAK_DETECTION: "true"
  script:
    - ./gradlew pixel5Check --full-stacktrace
  artifacts:
    reports:
      junit: src/build/outputs/androidTest-results/managedDevice/debug/pixel5/TEST-*.xml
    when: always
    paths:
      - src/build/reports/androidTests/managedDevice/debug/pixel5/index.html
      - src/build/reports/androidTests/managedDevice/debug/pixel5/**/**
      - src/build/outputs/androidTest-results/managedDevice/debug/pixel5/**/**
      - src/build/intermediates/managed_device_android_test_additional_output/pixel5/debug/**/**

# Deploys an apk to:
# https://gitlab.com/wallywallet/wallet/-/jobs/artifacts/main/wally.apk?job=deployAndroidApk
deployAndroidApk:
  <<: *skip-on-schedule
  needs:
    - androidPixel5Test
  script:
    - ./gradlew assembleRelease
    - cp src/build/outputs/apk/release/src-release.apk wally.apk
  artifacts:
    paths:
      - wally.apk
  rules:
    - if: '$CI_COMMIT_TAG =~ /^release[0-9]+\.[0-9]+\.[0-9]+$/'

testflight:
  rules:
    # Never run on scheduled pipeline used to mirror to github
    - *skip-on-schedule-fragment
    - if: $CI_COMMIT_REF_PROTECTED == "true"
  stage: deploy_internally
  resource_group: testflight-deployment
  tags:
    - m1-baremetal
  script:
    - mkdir iosApp/fastlane/keys
    # setting up ".env" file
    - echo "APP_STORE_CONNECT_API_KEY_PATH=$APP_STORE_CONNECT_API_KEY_PATH" >> iosApp/.env
    - echo "APPLE_ID=$APPLE_ID" >> iosApp/.env
    - echo "ITC_TEAM_ID=$ITC_TEAM_ID" >> iosApp/.env
    - echo "TEAM_ID=$TEAM_ID" >> iosApp/.env
    # setting up fastlane keys: json
    # see this for the correct quoting
    # https://docs.gitlab.com/ee/ci/yaml/script.html#use-special-characters-with-script
    - echo $KEYSJSON | base64 --decode > iosApp/fastlane/keys/$key_id.json
    # setting up fastlane keys: p8
    - echo $KEYSP8 | base64 --decode > iosApp/fastlane/keys/$key_id.p8
    # Run fastlane
    - cd iosApp/
    - gem install bundler
    - gem install fastlane
    - bundle install
    - bundle update
    - bundle exec fastlane ios testflight_internal --verbose

# CI lab that run on a scheduled pipeline on a daily base
mirror-to-github:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule" && $MIRROR == "True"'
      when: always
  image: alpine:latest
  stage: mirroring
  before_script:
    - apk add --no-cache openssh-client
    - apk add --no-cache git-lfs
    - mkdir -p ~/.ssh
    # NB the variable in CI settings has to be define as NOT protected variable
    # otherwise the value would not be exported on task that runs on branch which
    # are not protected. w/o setting this property to off is impossible to test if
    # the CI still works after future changes
    - echo "$SSH_GITHUB_MIRROR_PRIVATE_KEY" | base64 -d | awk '1; END {print ""}' > ~/.ssh/id_github_mirror
    - chmod 600 ~/.ssh/id_github_mirror
    - ssh-keyscan -H github.com >> ~/.ssh/known_hosts   # or use GIT_SSH_COMMAND later
    - echo "Host github*" >> ~/.ssh/config
    - echo "  HostName github.com" >> ~/.ssh/config
    - echo "  User Nexamoney" >> ~/.ssh/config
    - echo "  IdentitiesOnly yes" >> ~/.ssh/config
    - echo "  IdentityFile ~/.ssh/id_github_mirror" >> ~/.ssh/config
  script:
    # Even thou the current path store a cloned version of the wallywallet main repo
    # we need to clone the repo again cause by default gitlab use `--limit` flag to
    # trim the history to the last X commits, but we need the entire history of the repo
    # cause `git-lsf` will search for *.blend in all commits and will rewrite those.
    - git clone https://gitlab.com/wallywallet/wallet
    - cd wallet
    - git lfs install
    - git lfs track "media/*.blend"
    - git lfs migrate import --include="media/*.blend" --everything --yes
    - git remote add github-mirror "git@github.com:Nexamoney/wallywallet.git"
    - git fetch github-mirror
    - git push --force -u github-mirror main

#testflight_public:
#  environment:
#    name: "beta deploy"
#  allow_failure: false
#  only:
#    - main
#  when: manual
#  stage: deploy_beta
#  tags:
#    - m1-baremetal
#  script:
#    - mkdir iosApp/fastlane/keys
#    # setting up ".env" file
#    - echo "APP_STORE_CONNECT_API_KEY_PATH=$APP_STORE_CONNECT_API_KEY_PATH" >> iosApp/.env
#    - echo "APPLE_ID=$APPLE_ID" >> iosApp/.env
#    - echo "ITC_TEAM_ID=$ITC_TEAM_ID" >> iosApp/.env
#    - echo "TEAM_ID=$TEAM_ID" >> iosApp/.env
#    # setting up fastlane keys: json
#    # see this for the correct quoting
#    # https://docs.gitlab.com/ee/ci/yaml/script.html#use-special-characters-with-script
#    - echo $KEYSJSON | base64 --decode > iosApp/fastlane/keys/$key_id.json
#    # setting up fastlane keys: p8
#    - echo $KEYSP8 | base64 --decode > iosApp/fastlane/keys/$key_id.p8
#    # Run fastlane
#    - cd iosApp/
#    - gem install bundler
#    - gem install fastlane
#    - bundle install
#    - bundle update
#    - bundle exec fastlane ios beta_public --verbose
